#converting number  into  float
a=20
print(float(a))
print(str(a))

#Sets are nothing but the list which does not contains duplicate elements
#sets are mutable


a=[1,2,2,3]

print(set(a))

#frozensets are immutable
print(frozenset(a))

a[2]=4
